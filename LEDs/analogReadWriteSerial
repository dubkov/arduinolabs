int value = 0;
static int ledPin;

void setup (){
  Serial.begin (9600);  
}


void loop(){
  int value = analogRead (A0);
  value=map(value,0,1023,0,255);
  analogWrite(ledPin,value);
  Serial.println (value);
  delay(30);
}
